# 数字孪生地铁灾害应急仿真系统 - 项目架构说明
**PROJECT_ARCHITECTURE**
更新时间：2026-04-01

> 维护说明：当前对外执行基线已迁移到 `SYSTEM_DETAIL.md`、`ENV_SETUP.md`、`VISUAL_TEST_RUNBOOK.md` 与 `NOTES_AND_DECISIONS.md`。本文件保留为阶段性架构沉淀与问题复盘材料。

## 1. 整理与归档说明
针对进入中期开发与测试阶段，我们已经将此前所有的头脑风暴记录、会议总结、废弃的错误模板、以及项目搭建初期的临时探索脚本，统一归档转移至本项目的 `PROJECT/DOCS/ARCHIVE_PHASE1/` 目录下。

目前，**项目根目录**仅保留最核心的代码工程子系结构，而该文档则作为整套联调运转的全局指南，确保所有人对接时了解各子工程所负责的具体范围。

---

## 2. 系统宏观架构 (Macro Architecture)

整个系统采取 **“三层+流媒体跨源”** 的核心架构模式：

1. **表现层 (UE PixelStreaming & 前端 UI 面板)**
2. **调度通信层 (Node.js Signal Server)**
3. **业务与持久化层 (Node.js Middleware & MySQL Database)**

### 核心功能与端口绑定
为了避免内网冲突并统一全组测试基准，当前系统固定端口规划如下：
- **`8080`**: 前端信令 Web 服务面板端口 (HTTP/WebSocket - 被浏览器访问)
- **`8888`**: UE PixelStreaming 推流信令端口 (WebSocket - 被 UE 程序和 Cirrus 等待连接)
- **`3100`**: 中台业务接口端口 (REST API - 前端抓取或下发核心业务逻辑数据)
- **`3306`**: MySQL 本地数据库 (用于落库仿真记录和帧数据)

---

## 3. 子系统详细构成与代码分布 (Micro Layout)

当前 `PROJECT/` 下共包含三个工程目录，按职责划分如下：

### 3.1 表现层：虚幻引擎工程 (`UE_PROJECT/`)
本目录包含打包好的 UE 客户端以及源码。UE 程序在这里通过激活内置的 Pixel Streaming 插件充带物理模拟和渲染的角色，也是系统的数据呈现终端。
- **角色关系**：UE 程序是“画面生产者”。通过 `-PixelStreamingIP=127.0.0.1 -PixelStreamingPort=8888 -AudioMixer -RenderOffscreen` 参数静默式推流连接到前端的信令服务器上。不能不挂参数直接启动，否则会导致丢流或无法与前端握手。

### 3.2 交互面板与信令层：前端服务 (`FRONT_UE/frontend/WebServers/SignallingWebServer/`)
这里是所有前端逻辑与 UI 发生的地方。主要基于 Epic 的 Cirrus 框架改造而来。
- **代码结构 (`www/` 和 `scripts/`)**：
  - **`player.html`**: 主力呈现面板，集成了原生的交互操作界面，叠加了大模型预案、历史记录分析、像素流画面的 DOM。  
  - **`history.html`**: 第二轮新增的历史数据独立页面，承载“左侧仿真列表 + 右侧时间顺序数据列 + 分析配置区”的细化功能。
  - **`www/history-page.css`**: 历史数据页面样式，负责独立布局与响应式适配。
  - **`scripts/app.js`**: Epic 的原生像素流控制库，负责底层 WebRTC（ `webRtcPlayerObj` ）和对 UE 控制信号交互。
  - **`scripts/app-shell.js`**: 新加入的业务封装。它监听 UE 流状态、轮询查询 HTTP 服务器（比如检查 3100 的心兆），并负责解挂与展示加载遮罩层。
  - **`scripts/history-page.js`**: 历史数据页面逻辑，负责历史列表加载、时序数据采样展示以及分析配置交互占位。
- **角色关系**：通过 `socket.io` 同时向游览器分发信令并在后台通过 `8888` 端口同 UE 进程“牵红线”，让浏览器端的 WebRTC 可以直连 UE 的画面输出管道。

### 3.3 业务与持久化层：中台服务 (`MIDDLEWARE/server/` & `MIDDLEWARE/db/`)
中端采用标准的 Node+Express 作为接口服务，下连 MySQL 数据库作持久化。
- **作用**：提供所有高阶任务的处理（如新建仿真事件记录、执行疏散帧数据存储）。前端 UI 上的所有统计分析面板数值，不是直接由像素流传回，而是通过 HTTP/Socket 从 `3100` 端口请求此中台得到的。
- **角色关系**：为前端面板提供“大脑”支持。前端创建动作发给中台 -> 中台记录入库并生成 ID -> 中台或前端进一步发信令给 UE 完成引擎内的行为变化。

---

## 4. 近期关键技术问题的解决总结

在前面的重构联调过程中，我们遇到了环境迁移与网络握手造成的若干异常，目前均已排查攻克，具体解决思路记录如下：

### 4.1 端口错乱与 Socket.io 相关 404
- **问题表现**：前期整理将信令服务迁移至正规的包内后，网页会找不到 `/socket.io.min.js`；且默认的 HTTP 和 Streamer 端口相互挤占。
- **解决方案**：通过修改 `config.json`，把面向用户的网页面板严格指派给 `8080`，把接受 UE 连接的信令端口分拨给 `8888`；手动补全了项目中遗失的 `socket.io.min.js`，避免加载依赖时抛出 MIME 与 404 错误。

### 4.2 中文字符编码腐败吞噬闭合标签（白屏问题）
- **问题表现**：在重构过程中备份的 HTML 代码因为 Windows 环境或转移时字节丢失，触发了多字节文本腐败（Mojibake, 例如：`数字孪生仿真系?/title>` 使得 `<title>` 及之后的标签由于“`<`”被吞噬而无法成功闭合，引发整个前端 DOM 树解析崩溃，变为彻底白屏。
- **解决方案**：放弃存在各种转义错乱坑点的正则表达式，编写了独立的 Node.js 修复脚本（`fix-html.js`）。利用逐字节的正则表达式对残缺的乱码位置（如 `\ufffd` 组合）进行锚定清洗并重写了原 UTF-8 文本的中文节点，保住了其余数百行的 HTML 与绑定事件。

### 4.3 “等待像素流连接” 遮罩层卡死点击（无法操作）
- **问题表现**：虽然网络都通了且视频出画面了，但界面依然覆盖着黑色弹窗“等待ue像素流连接”，此时无法点击底层任何像素流画面相关的交互。
- **解决方案**：
  1. **变量级修正**：原厂脚本 `app.js` 将实例化变量（如 `webRtcPlayerObj` 和 `ws`）用 `let` 隔离在了局部作用域中，导致我们外壳的业务代码 `app-shell.js` 认为 `window.webRtcPlayerObj` 永远是 `undefined` 并拒绝消隐遮盖层。我们通过将其改成 `var` 放归全局作用域，确保能侦测到连接成功。
  2. **CSS级加固**：在 `app-shell.css` 里给遮罩容器 `.player-placeholder` 类施加了指令 `pointer-events: none;`，以确保在任何边界情况下，这层提示都不会窃取或拦截玩家向 UE 发送按键/鼠标信号。

### 4.4 历史数据细化页面（第二轮开发新增）
- **目标**：将历史数据能力从主页面弹窗解耦为独立子页面，形成可扩展的“数据回看 + 分析配置”工作区。
- **当前实现边界**：
  1. 历史列表：调用 `GET /api/simulations` 展示可选仿真记录；
  2. 时序数据列：基于 `GET /api/simulations/:simId/info` 获取时间范围后，按时间采样调用 `GET /api/simulations/:simId/frame?time=...` 生成演示型表格；
  3. 分析区：提供图表类型切换（曲线图/热点图/柱状图/散点图）与曲线图轴配置交互（横轴单选、纵轴多选）；
  4. 绘图引擎暂未接入，当前仅保留可执行交互链路与结果回显。

## 5. 项目进入下一阶段的保障
现在，本级项目结构已整备为高度紧凑且清除了绝大多数配置、编码及语法死锁问题。启动测试流程已被简化且记录在 `VISUAL_TEST_RUNBOOK.md` 中。开发人员可以在根部保留的这份清爽的目录树基础上，无缝进入下一步的前后端与中端具体大模型业务对接（如执行历史数据比对、大模型预案等功能块的攻坚）。